/**
 * Concataine first and last name
 * @param firstName string
 * @param lastName string
 * @returns string
 */
function getFullName(firstName: string, lastName: string): string {
  return `${firstName} ${lastName}`;
}

export default getFullName;
